module Main;

import TestModule;
import Main;

variant Nat {
    Zero;
    Succ {
        n: Nat;
    };

    proc add(other: Nat) : Nat {
        return Main::Nat::Zero;
    }
}

proc add(x : Nat, y : Nat) : Nat {
    return Nat::Zero;
}

proc main(x : Nat) : TestModule::Test {
    return TestModule::Test::Foo;
}