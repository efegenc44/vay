module Main

import Numbers (Nat as NatModule (Nat (Succ as inc, Zero)))

fun main() : NatModule::Nat = id(foo(
    inc(inc(Zero)),
    inc(Zero)
))

fun id:(T)(x : T) : T = x

fun foo(x : Nat, y : Nat) : Numbers::Nat::Nat =
    x
        .add(y)
        .mul(inc(inc(inc(Zero))))
