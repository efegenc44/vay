module Inter

interface Eq t {
    fun eq(that : t) : Bool
}

interface Add t {
    fun add(that : t) : t
}

variant Bool {
    True
    False

    fun eq(this, that : Bool) : Bool = that

    fun add(this, that : Bool) : Bool = Bool::True
}

