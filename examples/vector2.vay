module Main

struct Vector2(T) {
    x : T
    y : T

    fun:(T (Addable)) add(this, that : Vector2(T)) : Vector2(T) = (
        Vector2(
            this.x + that.x,
            this.y + that.y
        )
    )
}

fun main() : Vector2(Array(Char)) =
    Vector2("a", "b") + Vector2("c", "d")