module Main

import Numbers (Nat (Nat (Succ, Zero)))
import Core (Addable)

struct Vector2(T) {
    x : T
    y : T

    fun:(T (Addable)) add(this, that : Vector2(T)) : Vector2(T) = (
        Vector2(
            this.x.add(that.x),
            this.y.add(that.y)
        )
    )
}

fun main() : Vector2(Nat) =
    Vector2(Zero, Succ(Zero)) + Vector2(Succ(Succ(Zero)), Zero)