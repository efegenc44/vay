module Main

import Numbers (Nat (in Nat))

struct Test {}

fun main() : Nat =
    let I   = fun(x) x in
    let add = fun(x, y) Addable::add(x, y) in (
        I(())
        I(Test())

        Succ(Zero) + Succ(Succ(Zero))
    )

