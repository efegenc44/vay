module Interfaces

variant Ordering {
    Greater
    Equal
    Less
}

interface Ord t (Eq) {
    fun ord(that : t) : Ordering
}

interface Eq t {
    fun eq(that : t) : Bool
}

interface Add t {
    fun add(that : t) : t
}

variant Bool {
    True
    False
}

